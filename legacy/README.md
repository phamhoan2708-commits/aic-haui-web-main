# Legacy application

Website Flask/static trước migration được giữ tại đây để đối chiếu lịch sử. Thư mục này không tham gia lint, build hoặc Docker image.

`index.html` legacy vẫn có trong lịch sử Git tại commit `8f47516`; root `index.html` hiện là entry của Vite.
